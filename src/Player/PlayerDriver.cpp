#include "Player.h"
#include <iostream>

void testPlayers() {
    // Create some continents
    // Continent* asia = new Continent("Asia");
    // Continent* europe = new Continent("Europe");

    // // Create some territories
    // Territory* china = new Territory("China", asia);
    // Territory* india = new Territory("India", asia);
    // Territory* france = new Territory("France", europe);
    // Territory* germany = new Territory("Germany", europe);

    // // Create a player and add territories
    // Player player;
    // player.addTerritory(china);
    // player.addTerritory(india);
    // player.addTerritory(france);
    // player.addTerritory(germany);

    // // Get territories to defend and attack
    // std::vector<Territory*> toDefend = player.toDefend();
    // std::vector<Territory*> toAttack = player.toAttack();

    // std::cout << "Territories to defend: " << toDefend.size() << std::endl;
    // std::cout << "Territories to attack: " << toAttack.size() << std::endl;

    // // Clean up
    // delete china;
    // delete india;
    // delete france;
    // delete germany;
    // delete asia;
    // delete europe;
}
